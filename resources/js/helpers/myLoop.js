export default class MyLoop{
  constructor(times){
    this.times_remaining = times;
    this.running = true;
    this.next = () => {
      // shouldDelay?
      //shouldContinue?
      //Continue / Stop
    }
  }
}
